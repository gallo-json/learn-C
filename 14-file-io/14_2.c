/* Although the excercise asks us to copy a file, we cannot use and modify our `cp` mockup
   because copy.c treats all files as binary. 
   In this case we want to expand the spaces to tabs, meaning that the file we are manipulating must inherently be ASCII.
*/